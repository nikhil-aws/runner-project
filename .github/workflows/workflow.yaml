name: Hello World
on: [push]
jobs:
  Hello-World-Job:
#    runs-on:
#      - codebuild-runner-type-new-cb-${{ github.run_id }}-${{ github.run_attempt }}
#        image:aws/codebuild/amazonlinux-x86_64-standard:5.0-23.05.22
    runs-on:
      - codebuild-runner-type-new-${{ github.run_id }}-${{ github.run_attempt }}
        image:aws/codebuild/amazonlinux-x86_64-standard:5.0-23.05.22
    steps:
      - run: echo "Hello World"
